spring:
  config:
    import: optional:classpath:./env-dev.properties
  devtools:
    restart:
      enabled: true
  datasource:
    url: jdbc:postgresql://${DB_URL:localhost:5432}/${DB_NAME:cinema-db}
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:root}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        schema_update:
          unique_constraint_strategy: RECREATE_QUIETLY
  flyway:
    baseline-on-migrate: true
    baseline-description: init
    baseline-version: 0
    enabled: true
    user: ${DB_USER:root}
    password: ${DB_PASSWORD:root}
  mail:
    host: smtp.gmail.com
    port: 587
    sender: '"MiranhaCar" <eduardo.malagutti@catijr.com.br>'
    username: eduardo.malagutti@catijr.com.br
    password: tjxselqjuslhtvfd
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com

logging:
  level:
    root: INFO
